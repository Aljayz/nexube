[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "desk-vid-dl"
version = "0.1.0"
description = "yt-dlp based HLS downloader for nexube"
requires-python = ">=3.10"
license = { text = "MIT" }
